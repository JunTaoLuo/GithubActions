name: GA test
on:
  pull_request:
    types:
    - closed:
    branches:
    - 'main'
    - 'dev'

jobs:
  Test-Job:
    runs-on: ubuntu-20.04
    steps:
      if: ${{ github.event.pull_request.merged == true }}
      runs: echo Success!
